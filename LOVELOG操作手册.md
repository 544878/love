# LoveLog 完整操作手册

本手册适用于 LoveLog 网页版。LoveLog 是一个双人共享的学习、生活和关系记录应用，默认将数据保存在当前设备；登录共享空间后，双方记录才会同步到自建服务器。

## 1. 开始使用

### 1.1 首次打开

1. 使用 Chrome、Edge、Safari 等现代浏览器打开 LoveLog。
2. 点击“开启洳的今日空间”完成初始化。
3. 进入“设置”，先配置双方姓名、当前使用者和头像。
4. 如需双方跨设备共享，再配置“云同步”；仅在一台设备使用时可跳过。

### 1.2 主导航

- **今日**：查看天气、每日寄语、今日任务和快捷入口。
- **互动**：进入聊天、朋友圈、投喂愿望、共同课表和共同事件。
- **学习**：记录单词、做题、专注计时、待办和每日学习任务。
- **成长**：查看累计学习、打卡、徽章和双方赠送的成长奖励。
- **设置**：切换使用者、修改资料、配置 AI、云同步和数据备份。

页面会记住最近打开的位置和日期。

## 2. 双方身份与头像

打开“设置”。

### 2.1 切换当前使用者

在“当前空间”中选择“洳的空间”或“陶陶的空间”。当前使用者会影响：

- 学习记录的归属；
- 朋友圈发布者、点赞和评论身份；
- 聊天消息发送者；
- 共同课表新增任务的默认归属；
- 个人任务和成长数据的展示。

### 2.2 修改姓名与资料

- “洳的专属信息”可修改姓名、体重、目标院校、目标方向和天气备用城市。
- “陪伴者信息”可修改另一方姓名和昵称。

### 2.3 自定义头像

在“聊天与朋友圈头像”中点击任意一方头像，选择本机图片。头像会用于：

- 聊天消息；
- 朋友圈封面和动态；
- 互动页入口。

头像原图保存在本机照片数据库中；导出完整备份时会一并保存。

## 3. 今日页面

“今日”用于快速了解当天状态。

- 点击日期可查看对应日期。
- 天气卡会优先使用定位；定位不可用时使用设置中的备用城市。
- 自动化提醒会展示天气、陪伴寄语、吃饭提醒和学习复盘。
- 快捷入口可进入学习、日历、成长或互动页面。
- 当天完成的任务和记录会自动进入相关统计。

浏览器通知需要手动授权。若拒绝授权，应用内记录仍然正常工作。

## 4. 学习页面

### 4.1 单词学习记录

1. 输入“新学单词”数量。
2. 输入“复习单词”数量。
3. 可填写词书章节或学习感受。
4. 点击“收下这次进步”。

页面会显示今日总量、本周新学、本周复习和最近记录。记录按当前使用者区分。

### 4.2 专注计时

1. 点击“选择任务并开始”。
2. 填写本次学习标题和备注。
3. 开始后可暂停、继续或“结束并记录”。

结束后，实际时长会保存为学习记录，并进入今日总结和成长统计。

### 4.3 做题记录

填写科目或题目类型、题数和用时，点击“保存做题记录”。题数必须大于 0。

### 4.4 今日待办

- 输入任务后点击加号新增。
- 点击任务可切换完成状态。
- 待办按日期保存，适合一次性事项。

### 4.5 每日任务模板

- “新模板”用于建立重复任务，可设置名称、分组、目标和单位。
- 模板建立后，点击模板标签即可生成当天任务。
- “今日临时”用于只在当天出现的任务。
- 每条当天任务可以完成、编辑或移除。

### 4.6 今日学习小结

系统会汇总当天的专注、单词、做题和已完成学习任务。若已配置 AI，也可在 AI 学习助手中生成更自然的总结。

## 5. AI 学习助手

从学习页右上角进入 AI 助手。

### 5.1 助手类型

- **英语导师**：长难句、翻译、文本细读、文学背景和训练题。
- **法语老师**：法语入门、词汇、发音与对话练习。
- **生活助手**：学习安排、饮食建议和低压力任务拆分。

### 5.2 对话

- 每个助手按日期保存多组对话。
- 可创建新对话并切换历史会话。
- 支持 Markdown、表格和数学公式展示。

### 5.3 图片和文件

- 图片可使用千问视觉模型识别；未配置千问时会尝试本地 OCR。
- 文本类文件可进入本地知识库。
- 知识库文件支持编辑、重新建立索引和移除。

### 5.4 AI 配置

在“设置 > AI 学习助手”中配置：

- DeepSeek API Key、Base URL、模型和上下文参数；
- 千问 API Key、文字模型、视觉模型、OCR、向量、重排和语音参数；
- Serper API Key，用于联网搜索；
- 文件上传、联网搜索、重排检索和语音输出开关。

API Key 只保存在当前设备，不进入云同步或导出备份。

## 6. 聊天与朋友圈

### 6.1 聊天

在“互动”点击“进入我们的聊天”。

- 支持文字和图片。
- 消息按当前使用者区分左右方向。
- 云同步开启时会显示发送状态。
- 页面会自动滚动到最新消息。

### 6.2 朋友圈

在“互动”点击“我们的朋友圈”。

- 点击右上角相机发布动态。
- 支持文字、心情和多张图片。
- 双方可以点赞、取消点赞和评论。
- 点击动态图片可全屏预览。

## 7. 投喂愿望

在“互动 > 投喂指南”中：

1. 填写想吃的内容。
2. 填写口味、忌口或地点。
3. 选择“随缘”“想吃”或“现在就想”。
4. 点击“告诉她”。

点击愿望卡会依次切换“等待接单”“已接单”“已送达”。

## 8. 双人校园课表

### 8.1 时间规则

- 每天从 08:00 开始。
- 每节课 45 分钟。
- 每节之间休息 5 分钟。
- 每天提供 12 个节次。

### 8.2 新增课程或任务

1. 选择任务属于哪一方。
2. 选择星期和节次。
3. 填写课程或任务名称。
4. 可填写地点和本节目标。
5. 点击“写进这一周”。

两人的安排使用不同颜色显示。

### 8.3 周版本与历史

- 课表按周保存，不会覆盖历史周。
- 使用“上一周”“下一周”或周下拉框查看不同版本。
- 新一周没有记录时，可点击“沿用上周”复制上一周安排。
- 迁移前的旧课表显示为“旧版课表（只读归档）”，不会被自动删除。
- 当前周及新周记录可点击卡片右上角的“×”移除。

课表在手机上使用内部横向滚动，不会造成整页横向溢出。

## 9. 共同事件与纪念

在“互动 > 娱乐与纪念”中，可记录：

- 王者荣耀；
- 打电话；
- 见面；
- 纪念日；
- 约会；
- 自定义事件。

可以填写日期、开始结束时间和备注。未来事件会按时间排序显示，并可在日历中查看和补充回忆照片。

## 10. 日报、日历与成长

### 10.1 成长日报

日报可记录心情、学习时间、体重、饮食、生活笔记、留言和照片。保存后可以生成分享图片。

### 10.2 日历

日历集中查看日报、任务、共同事件和纪念记录。选择日期后可查看当天详情，并为共同事件补充文字和照片回忆。

### 10.3 成长

成长页展示：

- 连续记录天数；
- 累计学习时长；
- 学习会话；
- 系统徽章；
- 双方自定义徽章与奖励。

## 11. 云同步

### 11.1 创建共享空间

在第一台设备打开“设置 > 云同步”：

1. 输入用户名，至少 3 位。
2. 输入密码，至少 8 位。
3. 输入共享空间 ID，建议使用字母、数字、短横线或下划线。
4. 选择“创建共享空间”。
5. 登录后记下空间 ID 和邀请码。

首次登录会自动注册账号；服务器关闭注册后，只能使用已有账号。

### 11.2 加入共享空间

在另一台设备：

1. 输入另一方自己的用户名和密码。
2. 输入完全相同的共享空间 ID。
3. 选择“使用邀请码加入”。
4. 输入创建者提供的邀请码。
5. 登录。

双方均为平等成员，可读取和写入共享记录。

### 11.3 同步操作

- “上传本机”会把当前本机记录加入同步。
- “拉取云端”会获取服务器最新数据。
- 正常编辑后会自动排队同步。
- 页面可见且没有待上传记录时，会定期拉取聊天和互动更新。

本地 IndexedDB 始终是设备上的主要存储。断网时可继续使用，恢复网络后再同步。

## 12. 数据备份与恢复

### 12.1 导出备份

打开“设置 > 数据备份”，点击“导出完整备份”。备份包含：

- 本地业务记录；
- 设置；
- 本机照片和头像。

DeepSeek、千问和 Serper 密钥会在导出时清空。

### 12.2 恢复备份

点击“从备份恢复”，选择 LoveLog 导出的 JSON 文件。恢复操作会使用备份中的记录和照片替换当前本机数据，建议先导出当前备份。

### 12.3 清空本机数据

“清空本机全部记录”会删除当前浏览器中的 LoveLog 数据和照片，无法撤销。云端数据不会因此自动删除。

## 13. 网页开发与本地运行

环境要求：

- Node.js 当前 LTS；
- npm；
- Windows PowerShell、macOS Terminal 或 Linux Shell。

安装依赖：

```powershell
npm ci
```

启动前端开发服务器：

```powershell
npm run dev
```

开发环境会把 `/api` 代理到 `http://127.0.0.1:3000`。如需测试云同步，另开终端设置服务端环境变量后运行：

```powershell
npm run server
```

运行测试和网页构建：

```powershell
npm test
npm run build
```

预览生产网页：

```powershell
npm run preview
```

构建结果位于 `dist/`，该目录不提交 Git。

## 14. 服务端配置

复制 `server/.env.example` 中的变量到服务器进程环境：

```text
PORT=3000
HOST=127.0.0.1
LOVELOG_JWT_SECRET=至少32位随机字符串
LOVELOG_DATA_FILE=./server/data/lovelog.json
LOVELOG_ALLOW_REGISTRATION=true
LOVELOG_ALLOWED_ORIGINS=https://你的域名
```

注意：

- `LOVELOG_JWT_SECRET` 必须至少 32 位，否则服务拒绝启动。
- `LOVELOG_DATA_FILE` 包含账号密码哈希、共享记录和照片，必须备份且不得提交 Git。
- 同源部署可保持前端 `VITE_CLOUD_API_URL=/api`。
- 前后端分离时，在本机 `.env.local` 中填写完整 HTTPS API 地址，不要提交该文件。
- 完成账号创建后，可将 `LOVELOG_ALLOW_REGISTRATION` 改为 `false`。

## 15. 阿里云部署

推荐拓扑：

```text
浏览器 -> HTTPS 443 -> Nginx -> 127.0.0.1:3000 -> LoveLog Node 服务
```

仓库提供：

- `deploy/ALIYUN.md`：阿里云部署步骤；
- `deploy/lovelog.service.example`：systemd 服务模板；
- `deploy/nginx.conf.example`：Nginx 与 HTTPS 模板。

基本上线流程：

1. 将仓库部署到 `/opt/lovelog`。
2. 执行 `npm ci` 和 `npm run build`。
3. 创建专用用户与 `/var/lib/lovelog` 数据目录。
4. 将秘密写入 `/etc/lovelog/lovelog.env` 并限制权限。
5. 安装和启用 systemd 服务。
6. 配置 Nginx 和 HTTPS。
7. 使用 `curl https://你的域名/api/health` 检查，应返回 `{"ok":true}`。

不要把 Node 的 3000 端口直接暴露到公网。

## 16. 常见问题

### 页面一直显示旧版本

LoveLog 使用 PWA 缓存。刷新页面仍无变化时：

1. 完全关闭页面后重新打开。
2. 在浏览器开发者工具中清除该站点缓存。
3. 确认服务器已重新执行 `npm run build` 并重启。

### 云同步显示未配置

检查网页是否能访问 `/api/health`，以及前后端分离时 `VITE_CLOUD_API_URL` 是否为 HTTPS 地址。

### 登录失败

- 用户名至少 3 位；
- 密码至少 8 位；
- 加入空间时必须同时填写空间 ID 和邀请码；
- 服务器关闭注册后，新用户名不能自动创建。

### 图片无法上传

检查图片是否过大、浏览器存储空间是否充足，以及服务端反向代理是否允许至少 25 MB 请求体。

### AI 无响应

检查 API Key、Base URL、模型名称和网络。未配置或调用失败时，部分助手会返回本地兜底建议。

### 测试或构建失败

先执行：

```powershell
npm ci
npm test
npm run build
```

不要通过跳过测试或关闭 TypeScript 检查掩盖错误。

## 17. 隐私与维护约束

- 应用 ID 固定为 `com.nana.levelup`。
- 不提交 `.env.local`、API Key、Token、签名文件、`google-services.json` 或本机 SDK 路径。
- 不提交 `node_modules`、`dist`、服务端数据文件、Android 构建目录或 APK。
- 修改后至少运行 `npm test` 和 `npm run build`。
- 删除任何文件前，必须列出完整路径并获得明确确认。
- 生产数据文件和用户导出的备份应使用受控权限保存，并建立独立备份策略。
